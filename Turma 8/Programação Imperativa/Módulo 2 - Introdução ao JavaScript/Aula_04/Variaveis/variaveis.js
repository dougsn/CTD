let nomeDeUsuario = "Douglas";
console.log(nomeDeUsuario);


nomeDeUsuario = "Rafaella";
console.log(nomeDeUsuario);


if (true) {
  let nomeDeUsuario = "Diane";
  console.log(nomeDeUsuario)
}

// O let serve para definir uma variável em um âmbito de contexto, mudando se estiver em um contexto global ou local.

// o var serve para definir variaveis em um contexto global, não apenas em um contexto de bloco de código, por isso é mais utilizado o let

// o const é utilizado para definir uma 'variável' imutavel, que será constante.


const carro = 'Fiat'
console.log(carro);